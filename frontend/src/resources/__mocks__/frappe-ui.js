// Vitest stub for frappe-ui — only the runtime/API-calling exports need to be stubs.
// Pure helper functions (filterItems, matchesSearch) are co-located in items.js and
// do not depend on these, so empty stubs are sufficient for tests.
export const createListResource = () => ({})
export const createResource = () => ({})
